import './App.css'

//! 프로젝트 기초 환경 설정
// 1. 외부 라이브러리 설치 (의존성 설치)
// : axios
// : react-router-dom
// : zustand
// >> npm i + 각 라이브러리명 사용

// 2. 공통 상수 공통 함수, 공통 타입 명시

// 3. root 경로의 index.tsx에서 BrowserRouter 등록

// 4. 폴더 구조 생성
// apis(ts), components(tsx),constants(ts), layouts(tsx)
// , stores(ts), styles(ts), types(ts), views(tsx)

// : ts - export const tmp = '';
// : tsx - rfce(함수형 컴포넌트 생성)

function App() {
  return (
    <>
    </>
  )
}

export default App
